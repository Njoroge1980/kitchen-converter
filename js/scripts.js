var gallons = function (liters) {
    return liters*0.219969
    }
    var liters = parseInt(prompt("Enter amount in liters"));
    var result =gallons(liters);
    alert("the amount in gallons is " + result);
    undefined
    var pounds = function(kgs) {
    return kgs*2.205
    }
    var kgs = parseInt(prompt("Enter amount in kgs"));
    var result = pounds(kgs);
    alert("the amount in pounds is " + result);